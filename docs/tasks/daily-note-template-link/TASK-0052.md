# TASK-0052: 統合テスト + doc comments

**タスクID**: TASK-0052
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 3 - show拡張 + vim補完 + 統合テスト
**信頼性レベル**: 🔵 *既存統合テストパターン・受け入れ基準より*

## 関連文書

- **概要**: [overview.md](overview.md)
- **要件定義**: [requirements.md](../../spec/daily-note-template-link/requirements.md)
- **受け入れ基準**: [acceptance-criteria.md](../../spec/daily-note-template-link/acceptance-criteria.md)
- **設計文書**: [architecture.md](../../design/daily-note-template-link/architecture.md)

## タスク概要

`cli/tests/integration_test.rs` にS5機能の統合テストを追加し、全公開APIにdoc commentsを付与する。テンプレートCRUDのE2Eフロー、todayコマンドのワークフロー、リンク解決とバックリンク、テンプレート変数展開の統合テストを実装する。

## 依存タスク

- **前提タスク**: [TASK-0047](TASK-0047.md), [TASK-0048](TASK-0048.md), [TASK-0049](TASK-0049.md), [TASK-0050](TASK-0050.md), [TASK-0051](TASK-0051.md)
- **後続タスク**: なし

## 完了条件

- [ ] E2E テンプレートCRUD統合テスト
- [ ] E2E todayコマンド統合テスト
- [ ] E2E リンク解決 + バックリンク統合テスト
- [ ] E2E テンプレート変数展開統合テスト
- [ ] 全公開API (template.rs, template_engine.rs, link.rs) に doc comments
- [ ] `cargo test --workspace` 全パス
- [ ] `cargo clippy --workspace -- -D warnings` 通過
- [ ] 全テストがパス

## 実装詳細

### 1. テンプレートCRUD統合テスト 🔵

**信頼性**: 🔵 *受け入れ基準TC-101-01〜TC-101-05より*

```rust
#[test]
fn tc_s5_01_template_crud_lifecycle() {
    // 1. setup_vault() + unlock
    // 2. new_template("daily", "## {{date}}") → 成功
    // 3. list_templates() → 1件
    // 4. get_template("daily") → name=="daily", body=="## {{date}}"
    // 5. delete_template("daily") → 成功
    // 6. list_templates() → 0件
}
```

### 2. today コマンド統合テスト 🔵

**信頼性**: 🔵 *受け入れ基準TC-001-01〜TC-001-04より*

```rust
#[test]
fn tc_s5_02_today_new_and_existing() {
    // 1. setup_vault + unlock
    // 2. エントリなし → today処理 → "YYYY-MM-DD" エントリが作成される
    // 3. 再度today処理 → 同じエントリが返る（新規作成されない）
}

#[test]
fn tc_s5_03_today_with_daily_template() {
    // 1. setup_vault + unlock
    // 2. new_template("daily", "## {{date}} の記録")
    // 3. today処理 → テンプレート適用済みの本文
}
```

### 3. リンク解決 + バックリンク統合テスト 🔵

**信頼性**: 🔵 *受け入れ基準TC-201-01〜TC-201-05より*

```rust
#[test]
fn tc_s5_04_link_resolution_and_backlinks() {
    // 1. setup_vault + unlock
    // 2. new_entry("エントリA", "[[エントリB]] を参照", [])
    // 3. new_entry("エントリB", "Bの内容", [])
    // 4. resolve_links("[[エントリB]] を参照") → 1件解決
    // 5. backlinks_for("エントリB") → エントリA
}

#[test]
fn tc_s5_05_duplicate_title_link_resolution() {
    // 1. "メモ" タイトルのエントリを2件作成
    // 2. resolve_links("[[メモ]]") → 2件の候補
}
```

### 4. テンプレート変数展開統合テスト 🔵

**信頼性**: 🔵 *受け入れ基準TC-111-01〜TC-111-06より*

```rust
#[test]
fn tc_s5_06_template_variable_expansion() {
    // 1. extract_variables("## {{date}} - {{project_name}}") → 2件
    // 2. expand() with date="2026-04-05", project_name="Alpha"
    // 3. 結果が "## 2026-04-05 - Alpha"
}
```

### 5. Doc comments 🔵

**信頼性**: 🔵 *既存doc commentsパターンより*

- `core/src/template.rs`: TemplatePlaintext, TemplateMeta, TemplateName, CRUD関数
- `core/src/template_engine.rs`: VariableKind, VariableRef, extract_variables, expand
- `core/src/link.rs`: ParsedLink, ResolvedLink, BacklinkEntry, LinkIndex, parse_links
- `core/src/lib.rs`: 新規メソッド (new_template, list_templates, etc.)

## 単体テスト要件

統合テスト自体がテスト要件。追加の単体テストは不要。

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0052`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

## 注意事項

- 統合テストでは DiaryCore を直接使用（CLI層を迂回）
- テスト用の fast_params() (Argon2高速パラメータ) を使用
- LinkIndex の再構築テストでは unlock→操作→lock→unlock のサイクルをテスト
- doc comments は `///` で記述し、使用例を含める
- `cargo doc --workspace --no-deps` でドキュメント生成確認

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 5 | 0 | 0 | 5 |
| 統合テスト | 6 | 0 | 0 | 6 |

- 🔵 **青信号**: 11項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質

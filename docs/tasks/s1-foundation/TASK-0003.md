# TASK-0003: SecureBuffer実装

**タスクID**: TASK-0003
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *REQ-003・types.rs SecureBuffer定義・ヒアリングQ2より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/s1-foundation/requirements.md) - REQ-003, REQ-402, EDGE-101, EDGE-102
- **設計文書**: [📐 architecture.md](../../design/s1-foundation/architecture.md)
- **型定義**: [types.rs](../../design/s1-foundation/types.rs)
- **データフロー**: [dataflow.md](../../design/s1-foundation/dataflow.md) - SecureBufferライフサイクル

## タスク概要

Box<[u8]> ベースの SecureBuffer を実装する。Drop時に手動zeroizeを行い、メモリ再アロケートによるデータ残存リスクを排除する。空バッファと大規模バッファの境界値テストを含む。

## 依存タスク

- **前提タスク**: [TASK-0001: Cargoワークスペース構成](TASK-0001.md)
- **後続タスク**: [TASK-0004: ZeroizingKey・MasterKey・CryptoEngine型定義](TASK-0004.md)

## 完了条件

- [ ] core/src/crypto.rs に SecureBuffer が定義されている
- [ ] 内部型は Box<[u8]>（固定長、再アロケートなし）
- [ ] Drop実装でinner.zeroize()が呼ばれる
- [ ] ZeroizeOnDrop トレイトを実装
- [ ] new(), as_ref(), len(), is_empty() メソッドが存在
- [ ] 空バッファ(0バイト)のテストがパス
- [ ] 大規模バッファ(1MB)のテストがパス
- [ ] 全テストがパスする

---

## 実装詳細

### 1. SecureBuffer 構造体 🔵

**信頼性**: 🔵 *types.rs・ヒアリングQ2 (Box<[u8]>選択) より*

types.rs の設計に準拠。Vec<u8> を into_boxed_slice() で Box<[u8]> に変換して保持。

**実装ファイル**: `core/src/crypto.rs`

```rust
pub struct SecureBuffer {
    inner: Box<[u8]>,
}

impl SecureBuffer {
    pub fn new(data: Vec<u8>) -> Self {
        Self { inner: data.into_boxed_slice() }
    }
    pub fn as_ref(&self) -> &[u8] { &self.inner }
    pub fn len(&self) -> usize { self.inner.len() }
    pub fn is_empty(&self) -> bool { self.inner.is_empty() }
}

impl Drop for SecureBuffer {
    fn drop(&mut self) {
        self.inner.zeroize();
    }
}

impl ZeroizeOnDrop for SecureBuffer {}
```

### 2. lib.rs での公開 🔵

**信頼性**: 🔵 *architecture.mdモジュール構成より*

`pub mod crypto; pub use crypto::SecureBuffer;`

**実装ファイル**: `core/src/lib.rs`

---

## 単体テスト要件

### テストケース1: zeroize動作確認 🔵

**信頼性**: 🔵 *受け入れ基準 TC-003-01より*

**Given**: `SecureBuffer::new(vec![0xAA; 32])` を作成
**When**: Drop（スコープ終了）
**Then**: 内部データがゼロ化されている（unsafe テストで確認）

### テストケース2: 空バッファ(0バイト) 🔵

**信頼性**: 🔵 *受け入れ基準 TC-003-B01・ヒアリングより*

**Given**: `SecureBuffer::new(vec![])`
**When**: Drop
**Then**: パニックしない。is_empty() == true, len() == 0

### テストケース3: 大規模バッファ(1MB) 🔵

**信頼性**: 🔵 *受け入れ基準 TC-003-B02・ヒアリングより*

**Given**: `SecureBuffer::new(vec![0xFF; 1_048_576])`
**When**: Drop
**Then**: 正常にzeroize・Drop完了。len() == 1_048_576

### テストケース4: as_ref()でデータ読み取り 🔵

**信頼性**: 🔵 *types.rsのAPIシグネチャより*

**Given**: `SecureBuffer::new(vec![1, 2, 3])`
**When**: `sb.as_ref()`
**Then**: `&[1, 2, 3]` が返る

**テストファイル**: `core/src/crypto.rs` 内の `#[cfg(test)] mod tests`

---

## 実装手順

1. `/tsumiki:tdd-requirements s1-foundation TASK-0003` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- zeroize動作のテストでは unsafe を使って Drop 後のメモリを検証する必要がある。これはテストコードのみで許可（REQ-404はテストコードには適用されない）
- SecureBuffer は Send + Sync を自動実装（Box<[u8]> が Send + Sync のため）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 4 | 0 | 0 | 4 |

- **総項目数**: 6項目
- 🔵 **青信号**: 6項目 (100%)

**品質評価**: 最高品質

# TASK-0006: GitHub Actions CI設定

**タスクID**: TASK-0006
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *REQ-005・ヒアリングQ4・dataflow.md CIパイプラインフローより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/s1-foundation/requirements.md) - REQ-005, REQ-101, REQ-102
- **設計文書**: [📐 architecture.md](../../design/s1-foundation/architecture.md)
- **データフロー**: [dataflow.md](../../design/s1-foundation/dataflow.md) - CIパイプラインフロー

## タスク概要

GitHub Actions CI ワークフローを作成する。ubuntu-latest で cargo build, test, clippy, audit の4ステップを実行。push と pull_request トリガーで起動。

## 依存タスク

- **前提タスク**: [TASK-0001: Cargoワークスペース構成](TASK-0001.md)
- **後続タスク**: [TASK-0007: 統合ビルド検証](TASK-0007.md)

## 完了条件

- [ ] .github/workflows/ci.yml が存在する
- [ ] push / pull_request トリガーで起動
- [ ] ubuntu-latest で実行
- [ ] cargo build --workspace ステップが含まれる
- [ ] cargo test --workspace ステップが含まれる
- [ ] cargo clippy --workspace -- -D warnings ステップが含まれる
- [ ] cargo audit ステップが含まれる（cargo-audit のインストール含む）
- [ ] Rust toolchain キャッシュが設定されている

---

## 実装詳細

### 1. ci.yml ワークフロー定義 🔵

**信頼性**: 🔵 *REQ-005・architecture.md CIセクション・dataflow.md CIパイプラインフローより*

```yaml
name: CI
on:
  push:
    branches: [main, "sprint/**"]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --workspace
      - run: cargo test --workspace
      - run: cargo clippy --workspace -- -D warnings
      - run: cargo install cargo-audit --locked
      - run: cargo audit
```

**実装ファイル**: `.github/workflows/ci.yml`

### 2. ブランチフィルタ 🔵

**信頼性**: 🔵 *workflow.mdブランチ戦略より*

main と sprint/** ブランチへの push、main への PR でトリガー。

---

## 実装手順

1. `/tsumiki:direct-setup` - ci.yml ファイル作成
2. `/tsumiki:direct-verify` - YAML構文確認、ローカルで各コマンドが通ることを確認

---

## 注意事項

- cargo-audit のインストールは `--locked` フラグ付きで（再現性確保）
- rust-cache アクションでビルドキャッシュを使い、CI時間を短縮
- sprint/** ブランチも CI 対象に含める（sprint/s1 等）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |

- **総項目数**: 2項目
- 🔵 **青信号**: 2項目 (100%)

**品質評価**: 最高品質

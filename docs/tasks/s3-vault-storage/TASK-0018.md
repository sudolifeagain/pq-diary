# TASK-0018: vault/サブモジュール分割 + serde/toml依存追加

**タスクID**: TASK-0018
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤
**信頼性レベル**: 🔵 *architecture.mdより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: architecture.md - モジュール構成・依存関係セクション

## タスク概要

S1のvault.rsをサブモジュールに分割 (vault/mod.rs + format.rs + reader.rs + writer.rs + config.rs + init.rs)。serde, toml, uuid, chrono を core/Cargo.toml に追加する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-0019: vault.toml / config.toml serde構造体](TASK-0019.md), [TASK-0020: vault.pqd v4ヘッダ定数・構造体](TASK-0020.md)

## 完了条件

- [ ] core/src/vault.rs → core/src/vault/mod.rs に変換
- [ ] core/src/vault/format.rs 作成（空スケルトン）
- [ ] core/src/vault/reader.rs 作成（空スケルトン）
- [ ] core/src/vault/writer.rs 作成（空スケルトン）
- [ ] core/src/vault/config.rs 作成（空スケルトン）
- [ ] core/src/vault/init.rs 作成（空スケルトン）
- [ ] core/Cargo.toml に serde, toml, uuid, chrono 依存追加
- [ ] `cargo build --workspace` が成功
- [ ] 既存テストが全パス

---

## 実装詳細

### 1. vault.rs → vault/mod.rs 変換 🔵

**信頼性**: 🔵 *architecture.mdより*

vault.rs → vault/mod.rs に変換。pub mod 宣言で6サブモジュールを公開。

**実装ファイル**: `core/src/vault/mod.rs`

### 2. 空スケルトンモジュール5つ作成 🔵

**信頼性**: 🔵 *architecture.mdより*

format.rs, reader.rs, writer.rs, config.rs, init.rs を空スケルトンとして作成。

**実装ファイル**: `core/src/vault/format.rs`, `core/src/vault/reader.rs`, `core/src/vault/writer.rs`, `core/src/vault/config.rs`, `core/src/vault/init.rs`

### 3. Cargo.toml に依存追加 🔵

**信頼性**: 🔵 *architecture.md依存関係セクションより*

```toml
serde = { version = "1", features = ["derive"] }
toml = "0.8"
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
```

**実装ファイル**: `core/Cargo.toml`

---

## 実装手順

1. `/tsumiki:direct-setup` - モジュール分割・依存追加
2. `/tsumiki:direct-verify` - cargo build --workspace / 既存テスト確認

---

## 注意事項

- lib.rs の pub mod vault; と pub use は維持
- 既存のvault.rsの内容はmod.rsにそのまま移動

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)

**品質評価**: 最高品質

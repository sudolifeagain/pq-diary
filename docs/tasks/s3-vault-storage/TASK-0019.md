# TASK-0019: vault.toml / config.toml serde構造体 (config.rs)

**タスクID**: TASK-0019
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤
**信頼性レベル**: 🔵 *REQ-004, REQ-005・types.rs config構造体より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件**: REQ-004, REQ-005
- **型定義**: types.rs - config構造体セクション

## タスク概要

VaultConfig, AppConfig等のserde構造体を定義。Default実装。シリアライズ/デシリアライズ。ファイルI/O (from_file/to_file)。

## 依存タスク

- **前提タスク**: [TASK-0018: vault/サブモジュール分割 + serde/toml依存追加](TASK-0018.md)
- **後続タスク**: TASK-0025

## 完了条件

- [ ] VaultConfig 構造体定義 (serde Serialize/Deserialize derive)
- [ ] AppConfig 構造体定義 (serde Serialize/Deserialize derive)
- [ ] VaultConfig::default() / AppConfig::default() 実装
- [ ] from_file(path) → Result<T, DiaryError> 実装
- [ ] to_file(path) → Result<(), DiaryError> 実装
- [ ] 全テストパス (4テストケース)

---

## 実装詳細

### 1. VaultConfig 構造体 + Default 実装 🔵

**信頼性**: 🔵 *REQ-004・types.rsより*

vault.toml に対応するserde構造体。Vaultメタデータ（名前、バージョン、暗号パラメータ等）を保持。

**実装ファイル**: `core/src/vault/config.rs`

### 2. AppConfig 構造体 + Default 実装 🔵

**信頼性**: 🔵 *REQ-005・types.rsより*

config.toml に対応するserde構造体。アプリケーション全体の設定を保持。

**実装ファイル**: `core/src/vault/config.rs`

### 3. from_file / to_file 関数 🔵

**信頼性**: 🔵 *types.rsより*

TOML形式でのファイル読み書き。不正なTOMLの場合は DiaryError::Config を返す。

**実装ファイル**: `core/src/vault/config.rs`

---

## 単体テスト要件

### TC-004-01: VaultConfig シリアライズ→デシリアライズ一致 🔵

**信頼性**: 🔵

**Given**: VaultConfig インスタンス
**When**: TOML文字列にシリアライズし、デシリアライズ
**Then**: 元のインスタンスと一致

### TC-004-02: デフォルト値生成 🔵

**信頼性**: 🔵

**Given**: なし
**When**: VaultConfig::default() を呼び出し
**Then**: 期待されるデフォルト値が設定されている

### TC-005-01: AppConfig シリアライズ→デシリアライズ一致 🔵

**信頼性**: 🔵

**Given**: AppConfig インスタンス
**When**: TOML文字列にシリアライズし、デシリアライズ
**Then**: 元のインスタンスと一致

### TC-004-E01: 不正TOML → DiaryError::Config 🔵

**信頼性**: 🔵

**Given**: 不正なTOML文字列
**When**: from_file でパース
**Then**: DiaryError::Config が返る

**テストファイル**: `core/src/vault/config.rs` 内の `#[cfg(test)] mod tests`

---

## 実装手順

1. `/tsumiki:tdd-requirements s3-vault-storage TASK-0019` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- serde の derive マクロ (Serialize, Deserialize) を使用
- Default トレイト実装でデフォルト値を明確に定義
- ファイルI/Oエラーは DiaryError::Io に、パースエラーは DiaryError::Config にマッピング

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 4 | 0 | 0 | 4 |

- **総項目数**: 7項目
- 🔵 **青信号**: 7項目 (100%)

**品質評価**: 最高品質

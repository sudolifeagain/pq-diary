# TASK-0066: run-sprint.sh S6 設定更新

**タスクID**: TASK-0066
**タスクタイプ**: DIRECT
**推定工数**: 2h
**フェーズ**: Phase D - 統合
**信頼性レベル**: 🔵 *自動化基盤*

## 関連文書

- **概要**: [overview.md](overview.md)
- **スクリプト**: [run-sprint.sh](../../scripts/run-sprint.sh)

## タスク概要

`scripts/run-sprint.sh` の設定変数を Sprint 6 (search-stats-import) 用に更新する。SPRINT_NAME, SPRINT_TAG, TASKS 配列, TASK_TYPES 連想配列, TASK_NAMES 連想配列を S6 の全タスク一覧に合わせて書き換える。

## 依存タスク

- **前提タスク**: なし (並行可)
- **後続タスク**: なし

## 完了条件

- [ ] `run-sprint.sh` が S6 タスク一覧で実行可能
- [ ] `bash -n scripts/run-sprint.sh` で構文エラーなし

---

## 実装詳細

### 1. SPRINT_NAME, SPRINT_TAG 更新

**信頼性**: 🔵 *プロジェクト命名規約*

```bash
SPRINT_NAME="search-stats-import"
SPRINT_TAG="s6"
```

### 2. TASKS 配列更新

**信頼性**: 🔵 *S6 タスク一覧*

```bash
TASKS=(TASK-0053 TASK-0054 TASK-0055 TASK-0056 TASK-0057 TASK-0058 TASK-0059 TASK-0060 TASK-0061 TASK-0062 TASK-0063 TASK-0064 TASK-0065 TASK-0066)
```

依存順に全14タスクを列挙する。

### 3. TASK_TYPES 連想配列更新

**信頼性**: 🔵 *各タスクファイルの定義より*

```bash
declare -A TASK_TYPES=(
    [TASK-0053]="TDD"
    [TASK-0054]="TDD"
    [TASK-0055]="TDD"
    [TASK-0056]="TDD"
    [TASK-0057]="TDD"
    [TASK-0058]="TDD"
    [TASK-0059]="TDD"
    [TASK-0060]="TDD"
    [TASK-0061]="TDD"
    [TASK-0062]="TDD"
    [TASK-0063]="TDD"
    [TASK-0064]="TDD"
    [TASK-0065]="TDD"
    [TASK-0066]="DIRECT"
)
```

### 4. TASK_NAMES 連想配列更新

**信頼性**: 🔵 *各タスクファイルの定義より*

```bash
declare -A TASK_NAMES=(
    [TASK-0053]="vault reader/writer hardening"
    [TASK-0054]="password guard and hmac error handling"
    [TASK-0055]="EntryPlaintext zeroize and editor security"
    [TASK-0056]="VaultGuard drop guard pattern"
    [TASK-0057]="Win32 console unsafe ADR"
    [TASK-0058]="search core module"
    [TASK-0059]="search CLI command"
    [TASK-0060]="stats core module"
    [TASK-0061]="stats CLI and heatmap"
    [TASK-0062]="import core markdown parser"
    [TASK-0063]="import batch write and directory walk"
    [TASK-0064]="import CLI command"
    [TASK-0065]="integration tests and doc comments"
    [TASK-0066]="run-sprint.sh S6 config update"
)
```

### 5. 構文検証

**信頼性**: 🔵 *bash 標準検証*

更新後に以下を実行して構文エラーがないことを確認:

```bash
bash -n scripts/run-sprint.sh
```

---

## 実装手順

1. **direct-setup**: `scripts/run-sprint.sh` の SPRINT_NAME, SPRINT_TAG, TASKS, TASK_TYPES, TASK_NAMES を更新
2. **direct-verify**: `bash -n scripts/run-sprint.sh` で構文エラーなし確認

---

## 信頼性レベルサマリー

| 項目 | 信頼性 | 根拠 |
|------|--------|------|
| SPRINT_NAME/TAG | 🔵 | プロジェクト命名規約 |
| TASKS 配列 | 🔵 | S6 タスク一覧 |
| TASK_TYPES | 🔵 | 各タスクファイルの定義 |
| TASK_NAMES | 🔵 | 各タスクファイルの定義 |
| 構文検証 | 🔵 | bash -n 標準 |
